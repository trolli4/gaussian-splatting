name: gaussian_splatting
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - tqdm
  - plyfile
  - hdbscan
  - matplotlib
  - cmake
  - pip:
      - torch==1.13.1
      - torchvision==0.14.1
      - open-clip-torch
      - dearpygui
      - opencv-python
      - joblib==1.1.0
