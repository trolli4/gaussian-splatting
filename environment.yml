name: gaussian_splatting
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - plyfile=0.8.1
  - pip
  - tqdm
  - hdbscan
  - matplotlib
  - jupyter
  - pip:
    - dearpygui
    - opencv-python
    - open-clip-torch
    - joblib==1.1.0
